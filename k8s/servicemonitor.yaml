apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rudra-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: ros2   #✅ SAME label
  namespaceSelector:
    any: true
  endpoints:
    - port: http
      path: /metrics
      interval: 15s